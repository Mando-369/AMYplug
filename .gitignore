# Build output
/build/
/builds/
/dist/
*.o
*.obj
*.a
*.lib
*.exe

# Plugin bundles / artefacts
*_artefacts/
*.vst3
*.component
*.app
*.aaxplugin

# CMake
CMakeCache.txt
CMakeFiles/
cmake-build-*/
CMakeUserPresets.json

# IDEs
.idea/
.vscode/
*.xcodeproj/
*.xcworkspace/
.DS_Store

# Dependency caches (submodules are tracked; their build caches are not)
/third_party/**/build/
/third_party/**/*.o

# Local license cache fetched by bootstrap (the canonical AGPL text)
# (keep tracked once present; ignore only temp copies)
/LICENSE.full.tmp

# Local DX7 patch library for testing import (third-party, not distributed)
dx7_sounds/

# Local scratch audio captures (not source)
testaudio/
